package com.exclusivostars.app

object Config {
    // Backend local (Termux / mismo dispositivo, o la IP de tu PC en
    // la red). Cambialo acá si el servidor no está en localhost:
    // ej. "http://192.168.1.5:5000"
    const val BASE_URL = "http://127.0.0.1:5000"
}
