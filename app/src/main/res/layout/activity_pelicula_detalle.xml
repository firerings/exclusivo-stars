<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <androidx.core.widget.NestedScrollView
        android:id="@+id/scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="32dp">

            <!-- Hero: fondo atmosférico difuminado + tarjeta nítida,
                 ahora anclada ABAJO del hero (el nombre queda justo
                 debajo, fuera de este bloque) y casi a todo el ancho
                 —apenas márgenes mínimos a los lados—, igual criterio
                 que .pelicula-hero-thumb en la web (align-items:
                 flex-end + width: min(920px,74vw)). Reemplaza
                 FrameLayout por ConstraintLayout porque hace falta el
                 ratio 16:9 responsive (app:layout_constraintDimensionRatio),
                 imposible de lograr con FrameLayout/LinearLayout puro.
                 Mismo orden de prioridad fondo > tarjeta > poster que
                 pelicula.html. -->
            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="330dp">

                <ImageView
                    android:id="@+id/hero_fondo"
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:scaleType="centerCrop"
                    android:contentDescription="@null"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent" />

                <!-- Degradado hacia el fondo de la pantalla, igual criterio
                     que .pelicula-hero-degradado (web). -->
                <View
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:background="@drawable/hero_degradado"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent" />

                <!-- Tarjeta: arranca mostrando miniatura+play; al
                     tocar, el reproductor inline (player_view_inline)
                     la reemplaza EN EL MISMO lugar/tamaño — igual
                     criterio que _player.html embebido en la web (el
                     player ocupa el contenedor de la tarjeta, no
                     navega a otra pantalla). -->
                <FrameLayout
                    android:id="@+id/hero_tarjeta"
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    app:layout_constraintDimensionRatio="H,16:9"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    android:layout_marginStart="8dp"
                    android:layout_marginEnd="8dp"
                    android:layout_marginBottom="20dp"
                    android:background="@drawable/poster_placeholder_background"
                    android:clipToOutline="false">

                    <ImageView
                        android:id="@+id/hero_tarjeta_img"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null" />

                    <!-- Reproductor inline. surface_type texture_view
                         para que CropCorrector pueda aplicar la Matrix
                         de recorte, igual que en PlayerActivity. -->
                    <androidx.media3.ui.PlayerView
                        android:id="@+id/player_view_inline"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:visibility="gone"
                        app:surface_type="texture_view" />

                    <ImageView
                        android:id="@+id/hero_play_icon"
                        android:layout_width="60dp"
                        android:layout_height="60dp"
                        android:layout_gravity="center"
                        android:src="@drawable/ic_play_circle"
                        android:contentDescription="@string/reproducir" />

                    <ProgressBar
                        android:id="@+id/hero_inline_progress"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center"
                        android:visibility="gone" />

                    <!-- Expandir a pantalla completa: solo visible
                         mientras el inline está reproduciendo. -->
                    <ImageButton
                        android:id="@+id/hero_fullscreen_button"
                        android:layout_width="34dp"
                        android:layout_height="34dp"
                        android:layout_gravity="top|end"
                        android:layout_margin="8dp"
                        android:background="@drawable/back_button_background"
                        android:src="@android:drawable/ic_menu_crop"
                        app:tint="@android:color/white"
                        android:contentDescription="@string/pantalla_completa"
                        android:visibility="gone" />
                </FrameLayout>
            </androidx.constraintlayout.widget.ConstraintLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="20dp"
                android:paddingEnd="20dp"
                android:paddingTop="18dp">

                <TextView
                    android:id="@+id/titulo"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/text_primary"
                    android:textSize="24sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/meta"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:textColor="@color/text_secondary"
                    android:textSize="13sp" />

                <TextView
                    android:id="@+id/director"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:textColor="@color/text_secondary"
                    android:textSize="13sp"
                    android:visibility="gone" />

                <!-- Puntuación TMDB: versión simple (píldora con %) en vez
                     del anillo SVG de la web — el anillo real queda para
                     el pase visual (fase de paridad general). -->
                <TextView
                    android:id="@+id/puntuacion"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="10dp"
                    android:background="@drawable/puntuacion_background"
                    android:paddingStart="9dp"
                    android:paddingEnd="9dp"
                    android:paddingTop="5dp"
                    android:paddingBottom="5dp"
                    android:textColor="@color/gold"
                    android:textStyle="bold"
                    android:textSize="12sp"
                    android:visibility="gone" />

                <!-- Aviso de reproducción directa — misma copy que
                     .pelicula-aviso-directo en pelicula.html. -->
                <TextView
                    android:id="@+id/aviso_directo"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="11dp"
                    android:background="@drawable/aviso_directo_background"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:paddingTop="8dp"
                    android:paddingBottom="8dp"
                    android:textColor="@color/aviso_directo_texto"
                    android:textSize="12sp"
                    android:visibility="gone" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="26dp"
                    android:paddingBottom="10dp"
                    android:text="@string/seccion_sinopsis"
                    android:textColor="@color/text_primary"
                    android:textSize="15sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/sinopsis"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/text_secondary"
                    android:textSize="14sp"
                    android:lineSpacingMultiplier="1.3" />

                <TextView
                    android:id="@+id/reparto_titulo"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="26dp"
                    android:paddingBottom="10dp"
                    android:text="@string/seccion_reparto"
                    android:textColor="@color/text_primary"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    android:visibility="gone" />

                <!-- layoutManager horizontal se fija en código
                     (PeliculaDetalleActivity): el atributo XML
                     android:orientation no aplica a RecyclerView, solo a
                     LinearLayoutManager. -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recycler_reparto"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:clipToPadding="false"
                    android:visibility="gone" />

            </LinearLayout>
        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <ImageButton
        android:id="@+id/back_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="14dp"
        android:background="@drawable/back_button_background"
        android:contentDescription="@string/volver"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        app:tint="@android:color/white" />

    <ProgressBar
        android:id="@+id/progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center" />

    <LinearLayout
        android:id="@+id/error_state"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/error_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />

        <Button
            android:id="@+id/retry_button"
            android:layout_width="wrap_content"
            android:layout_height="46dp"
            android:layout_marginTop="16dp"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:text="@string/retry"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:background="@drawable/primary_button_background"
            android:backgroundTint="@null" />
    </LinearLayout>

</FrameLayout>
